{
  "name": "maularinn-cli",
  "version": "0.1.0",
  "module": "src/index.ts",
  "devDependencies": {
    "@biomejs/biome": "^2.4.9",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "bin": {
    "maul": "src/index.ts"
  },
  "private": true,
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "build": "bun build --compile src/index.ts --outfile maul",
    "test": "bun test",
    "lint": "bunx biome check src/",
    "lint:fix": "bunx biome check --write src/",
    "format": "bunx biome format --write src/",
    "typecheck": "bunx tsc --noEmit",
    "check": "bun run lint && bun run typecheck && bun run test"
  },
  "type": "module",
  "dependencies": {
    "amazon-cognito-identity-js": "^6.3.16"
  }
}
